# Folder for staging and prod migrations

This folder contains all common flyway migrations.

All contents of this folders are copied over to the final docker image and `sandbox.yml` has this
location by default.
